var fs = require("fs");
var path = require("path");

function relative(loc) {
  return path.join(__dirname, "..", loc);
}

fs.writeFileSync(relative("browser.js 1"), fs.readFileSync(relative("dist/polyfill.min.js 1")));
